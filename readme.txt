Usos actuales:
- Se puede usar para detectar cambios en vistas p_dmt_views unicamente, ya que en 2021-2022 no teniamos procesos fisicos.
- Si los campos no estan casteados, es decir figura así "NULL AS campo", entonces uno tiene que mirar manualmente las tablas fisicas e IPCs.
- Tiene una opcion para poder leer los archivos de estructuras de datio por si nuestros cambios estan bien y los de ellos mal, permite ver la diferencia entre nuestras
estructuras. Aunque esto no lo hemos necesitado hace mucho tiempo.

Tareas futuras:
- Agregar opcion que lea archivos XML de workflows para no tener que mirar que los puertos del mapping esten bien.
- Mejora para que mire el excel de Summary completo y no tener que guardar en un archivo a parte los campos que figuran en el Summary.
- Agregar opcion para tablas fisicas.
